@using AccessManagementLaredo;
@model IEnumerable<County>

<h1>
    Catalog of Counties
</h1>

<kendo-grid name="grid" height="550">
    <datasource type="DataSourceTagHelperType.Ajax" page-size="20" server-operation="true" on-error="fnShowError">
        <transport>
            <create url="/County/Create" />
            <read url="/County/Read" />
            <update url="/County/Update" />
            <destroy url="/County/Delete" />
        </transport>
        <schema>
            <model id="Id">
                <fields>
                    <field name="Id" type="number" editable="false"></field>
                    <field name="Name" type="String"></field>
                    <field name="Number" type="String"></field>
                    <field name="Contact" type="String"></field>
                </fields>
            </model>
        </schema>
    </datasource>
    <editable mode="inline" />
    <groupable enabled="true" />
    <sortable enabled="true" />
    <filterable enabled="true" />
    <pageable button-count="5" refresh="true" page-sizes="new int[] { 5, 10, 20 }">
    </pageable>
    <toolbar>
        <!-- Enable the built-in grid's Toolbar commands "create", "save", and "cancel". -->
        <toolbar-button name="create" text="Add new county"></toolbar-button> <!-- Adds an empty row to the grid to create a new record. -->
    </toolbar>
    <columns>
        <column field="Name" title="Name" />
        <column field="Number" title="Number" />
        <column field="Contact" title="Contact" />
        <column width="220">
            <commands>
                <column-command text="Edit" name="edit"></column-command>
                <column-command text="Delete" name="destroy"></column-command>
            </commands>
        </column>
    </columns>
</kendo-grid>

<kendo-dialog id="messageDialog" width="400" visible="false">
    <content></content>
</kendo-dialog>